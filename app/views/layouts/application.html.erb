<!DOCTYPE html>
<html>
<head>
  <title>MiGram</title>
   <%= stylesheet_link_tag    'https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css'%>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<nav class="navbar navbar-inverse nav-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span> 
      </button>
      <%= link_to "MiGram", root_url, :class=> "navbar-brand" %>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav">
        
        <li><%= link_to '<span class="glyphicon glyphicon-plus-sign"></span> New'.html_safe, new_post_path %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <%- if current_user %>
          <li>
            <%= link_to '<span class="glyphicon glyphicon-log-out"></span> Log Out'.html_safe, destroy_user_session_path, method: :delete, data: {confirm: "Are you sure?"}%>
          </li>
        <% else %>
          <li>
            
            <%= link_to '<span class="glyphicon glyphicon-log-in"></span> Login'.html_safe, new_user_session_path %>
          </li>
          <li>
            <%= link_to '<span class="glyphicon glyphicon-user"></span> Sign Up'.html_safe, new_user_registration_path %>
          </li>
          
        <% end %>
      </ul>
    </div>
  </div>
</nav>
  
<div class= "container text-center">

  <% flash.each do |a, b| %>
    <div class= "alert alert-success"><%= b %> </div>
  <% end %>
  
  <%= yield %>
  
</div>

</body>
</html>
